# proto-file: proto/stipulator/v1/records.proto
# proto-message: stipulator.v1.Gap

requirement_id: "REQ-proj-fidelity"
reason: "Kernel + FUSE arms pinned; the ProjFS backend and its windows suite are authored and cross-compiled but unexecuted (docs/issues/projfs-windows-validation.md), and the FSKit arm is unbuilt"
lands { manual { condition: "windows validation run reports back with findings dispositioned, and the FSKit backend lands and pins its arm" } }
