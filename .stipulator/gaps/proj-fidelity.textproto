# proto-file: proto/stipulator/v1/records.proto
# proto-message: stipulator.v1.Gap

requirement_id: "REQ-proj-fidelity"
reason: "The kernel and the FUSE backend pin the linux arm (tree, envelope, content reads, kernel-ro); the ProjFS and FSKit arms — their envelope columns, read paths, veto/error enforcement, and same-tree parity — land with those backends"
lands { manual { condition: "ProjFS and FSKit backends land and pin their fidelity, content, read-only, and same-tree clauses" } }
